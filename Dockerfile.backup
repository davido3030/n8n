# Simple n8n deployment for Railway
FROM n8nio/n8n:latest

# Railway expects PORT environment variable
ENV PORT=5678
ENV N8N_PORT=5678
ENV N8N_HOST=0.0.0.0

EXPOSE 5678

# Use default n8n startup
CMD ["n8n", "start"]